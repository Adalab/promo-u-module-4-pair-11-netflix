<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="http://beta.adalab.es/resources/stylesheets/all.css" />
  </head>
  <body>
    <section class="border--medium"> 
    <ul class="cards">
      <% if(foundMovie !== "") {%> <% foundMovie.forEach(function(item){ %>
 
      <li class="card">
        <h2 class="card__title"><%= item.title %></h2>
       <img class="card__img" src="<%= item.image %>" alt="" />
       <p class="card__description"><%= item.genre %></p>
        <!-- <p><%= item.category %></p>
        <p><%= item.year %></p> -->
      </li>
      <% }) %> <% } %>
    </ul>
    </section>
  </body>
</html>
